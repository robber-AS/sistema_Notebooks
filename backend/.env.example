# ============================================================
# CONFIGURAÇÃO DO BACKEND - copie para .env
# ============================================================

# Servidor
PORT=3000
JWT_SECRET=segredo_super_secreto_mude_isto
JWT_EXPIRES=8h

# Banco de dados MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sistema_notebooks
DB_USER=root
DB_PASS=

# Origem permitida (frontend). Use * para liberar todas
CORS_ORIGIN=*